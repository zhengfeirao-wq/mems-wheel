# 统一换行符，避免 Windows/Linux 协作时整文件 diff
* text=auto eol=lf

# 明确二进制，禁止 Git 触碰
*.hex   binary
*.bin   binary
*.whl   binary
*.pdf   binary
*.png   binary
*.jpg   binary
*.zip   binary
*.PcbDoc binary

# Windows 专用脚本保留 CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf
